apiVersion: v1
kind: Service
metadata:
  name: ecologistics-service
spec:
  type: NodePort
  selector:
    app: ecologistics
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
    - name: api
      protocol: TCP
      port: 4000
      targetPort: 4000
